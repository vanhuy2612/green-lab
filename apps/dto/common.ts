export enum OTPAction {
  DEFAULT = "DEFAULT",
  SIGN_UP = "SIGN_UP",
  FORGOT_PASSWORD = "FORGOT_PASSWORD",
}

export enum OrderBy {
  desc = "desc",
  asc = "asc",
}

export enum AreaListCitiesSortBy {
  code = "code",
  name = "name",
}
